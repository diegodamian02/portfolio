# Base URL of the Express backend (server/). Only used by the #my-taste
# section to read the owner's cached Spotify data — the iTunes-powered
# turntable hero calls iTunes directly from the browser and needs no backend.
VITE_API_BASE_URL=http://localhost:5050
