# Serves the Vite production build with client-side-routing fallback so a
# hard refresh on any React Router route (e.g. /about) doesn't 404.
# {$PORT} is injected by Railway at runtime.
:{$PORT} {
	root * dist
	encode gzip
	try_files {path} /index.html
	file_server
}
