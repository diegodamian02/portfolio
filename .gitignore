.vercel
node_modules
.env*
!.env.example

# Claude Code: .claude/settings.json is shared and tracked. Personal overrides
# (extra permissions, local env) belong in settings.local.json, which is not.
.claude/settings.local.json

# IDE / editor local state. .idea/workspace.xml is per-user session state
# (open tabs, change lists, run configs) and was tracked until 2026-08-08.
.idea/
.vscode/
*.iml

# macOS
.DS_Store

# Claude Design canvas — seeded payload is a large regenerable artifact (seed-canvas.mjs)
design-review/mobile-redesign/mobile-redesign.html
